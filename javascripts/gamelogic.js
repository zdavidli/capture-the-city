
for (var node in intersection) {
  if(node.fillColor != #000000) {
    
  }
}
function Node(){
  
}

Node.prototype.isClickable = function (node) {
    if(node.fillColor != #000000) {
      return false;
    }
    return true;
  }
